-- NOTE: The keymaps need to be loaded first because the leader settings
-- have to be set before plugins are loaded.
require("zrf.config.keymaps")
require("zrf.config.options")
require("zrf.colors")

require("zrf.plugins")
